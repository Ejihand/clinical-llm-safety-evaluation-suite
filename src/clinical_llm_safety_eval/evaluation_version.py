"""
Version strings for reproducibility.

Bump test_pack_version when you change test_cases.csv or model response fixtures.
Bump rubric_version when you change scoring rules in scoring.py.
The harness version is the package version in pyproject.toml (used for run manifests).
"""

from __future__ import annotations

TEST_PACK_VERSION = "1.1.0"
RUBRIC_VERSION = "1.0.0"
